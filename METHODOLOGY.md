# FinGuard 检测方法论与网站逻辑梳理

> **如何批注**：在任意要修改的地方插入 `【批注：你的意见】`，或用 `<!-- 批注: ... -->`。
> 改完保存，然后在对话里说"看批注"，我会逐条落地并同步代码 / `/methodology` 页面。
> 每节都编了号（如 §4.2），可直接引用。
>
> （本版已按你上一轮批注修订：删除「指标重算」整块、复核去掉监管高频/集团准则两块、
> 报告总览去掉「整体结论」、泡泡玛特示例改用真实**年度报告**数据并写明准则与核数师。）

---

## §1　一句话逻辑

用户上传一份 PDF 财报 → 系统做**三件产出** → 结果分**三个标签页**呈现：
**① 报告总览 / ② 数据复核 + 语法核查 / ③ 财务健康度分析**。
全部由**大模型**按固定规则执行：以"读文字、核勾稽、摆事实"为主，**判断权交给使用者**。

---

## §2　网站动线（页面与数据流）

```
首页(/)  →  工作台(/analyze)  →  解析过程  →  结果页(/results)
 介绍/方法论入口        上传PDF(可多选)+选模式    进度       三标签呈现
                                                        ↘ 存档到 历史(/history)
```

一次复核内部：

1. **`/api/extract`**：pdf-parse 抽文本（用于原文定位与文本回退路径），并返回页数等画像。
2. **`/api/analyze`**：多引擎，按 key 自动择优（优先级 Kimi > GLM > Claude > 文本；可用 `VISION_PROVIDER` 显式指定）：
   - **Kimi 路径（长报告首选）**：配 `MOONSHOT_API_KEY` 时启用。把 PDF 上传到 Moonshot `/files`(purpose=file-extract) 原生解析（含扫描件 OCR、表格还原），再用 256K 长上下文模型（默认 `kimi-k2.5`）流式复核——无需渲染，最适合 200+ 页年报。
   - **GLM-4.5V 视觉路径**：配 `ZHIPUAI_API_KEY` 时启用。服务端用 `pdf-to-img` 把 PDF 逐页渲染成图像，按 GLM 64K 上下文分批（默认 8 页/批，最多 40 页）送 `glm-4.5v` 逐页"看"版面并合并；适合中短报告，长报告会受 40 页上限截断。
   - **Claude 视觉路径**：配 `ANTHROPIC_API_KEY` 且 PDF ≤100 页 / ≤30MB 时，把 PDF 原件作为 document 块交给 Claude 原生读取。
   - **文本路径（兜底）**：无视觉 key 或 PDF 过大时，用 `pdf-parse` 抽文本送 DeepSeek / 火山方舟（长报告分块）。
   均流式产出「报告总览 + 数据复核 + 语法核查 + 财务健康度分析」（markdown）。
3. 结果存 sessionStorage + IndexedDB（历史），跳 `/results` 渲染。

辅助：`/api/feedback`（反馈：本地文件 + 可选 webhook / 邮件）、`/admin/feedback`（看反馈）、`/methodology`（能力清单）。

---

## §3　检测逻辑（穿透式复核）

只有一条主线：**大模型按固定模板做穿透式复核**——先识别报告画像，再分层核验勾稽与披露，最后做审慎的健康度评估。
强约束（写进系统提示）：只摆可独立验证的事实，**不对具名主体下"造假/舞弊"结论**；拿不准的归入"待管理层确认事项"；缺数据填"未披露/N\/A"，不编造。

---

## §4　三个产出（结果页三个标签）

### §4.1　报告总览
自动识别：报告名称、报告类型（注意：港股「年度业绩公告」与「年度报告」是两回事，按报告原文如实填写）、报告期间、**适用准则（写明单一准则：IFRS 或 HKFRS 或 CAS，不并列）**、审计机构 / 核数师、**签字会计师 / 项目合伙人**（港股按 engagement partner 如实填）、报告状态（正式 / 草稿）。
（已去掉原"整体结论"段。）

### §4.2　数据复核 + 语法核查（穿透式复核，最厚的一块）
分模块、由浅入深：

- **模块 A · 算术与跨表勾稽**：三大表逐层加总、资产=负债+权益、EPS 重算、现金流量表逐行勾稽、各表期初=上期期末。
- **模块 B · 附注与格式**：附注纵横双维核验、成本/累计折旧/账面净值三层、Note 序号连续性与"幽灵索引"、简称与正负号一致、报表线条规范、占位符 / DRAFT / 签字栏空白。
- **模块 C · 现金流量表逐行深度勾稽**：间接法非现金调节项、营运资本变动三步法、投资/筹资逐行与对账表核对。
- **模块 D · 语言合规**：中英文语系一致、术语规范、主谓一致、法定声明完整性。

每个问题输出统一卡片：类别 / 风险等级 / 位置 / 描述 / 证据链 / 影响 / 建议；
并分「明显问题（可直接处理）」与「待管理层确认事项（需外部信息）」。

### §4.3　财务健康度分析（7 维度）
盈利、偿债、现金流、营运、成长、重大异常波动、持续经营。
每维四档评估：✅ 未见异常 / 🟡 建议关注 / 🔴 显著风险 / ⚪ 数据不足。

---

## §5　贯穿始终的设计原则

1. **事实呈现，而非定性指控**：只摆事实与线索，不下"造假/操纵"结论，判断权交给用户。
2. **缺数据标"未披露 / N\/A"，不编造。**
3. **风险分级 + 两类问题**：明显问题 vs 待管理层确认。
4. **可追溯**：问题附证据链与原文片段定位。
5. **限于"已解析内容"**：长报告分块解析，不声称全文全量。

---

## §6　能力边界

- 只查**报告内部一致性、勾稽、披露完整性**；**查不出内部自洽的实质造假**（口径一致但数据本身是假的）。
- **不构成审计、鉴证、证券投资建议，不对任何主体作造假认定。**
- 配置视觉模型（Claude）后**支持扫描件 / 图片型 PDF**（逐页读版面）；超 100 页 / 30MB 自动回退到文本路径。
- 所有结论须经专业人员人工复核。

---

## §7　待你确认的合规红线（非代码，需你拍板）

1. **独立性死线**：对外版**绝不分析本所（KPMG）的审计客户**。
2. **兼职 / 利益冲突 / 品牌使用**：以在职身份对外运营，大概率需走所内审批。
3. **证券资质**：坚持"事实呈现、不给投资建议"，避免被认定为变相证券投资咨询。
4. **数据来源 ToS**：别批量爬巨潮 / Wind / HKEX，注意反爬与接口滥用条款。

---

<!-- 从这里往下自由写整体批注，或在上面各节内插入【批注：...】 -->
## §8　我的整体批注

-
